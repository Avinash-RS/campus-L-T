import { DateMonthYearDirective } from './date-month-year.directive';

describe('DateMonthYearDirective', () => {
  it('should create an instance', () => {
    const directive = new DateMonthYearDirective();
    expect(directive).toBeTruthy();
  });
});
