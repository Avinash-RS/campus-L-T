import { YearOnlyDirective } from './year-only.directive';

describe('YearOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new YearOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
